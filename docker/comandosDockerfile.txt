
docker logs --tail=80 db_postgres
docker volume ls
docker volume rm *name*
docker compose up -d
docker compose stop
docker compose start
docker ps
docker ps -a 
docker stop *name*
docker start *name*

Crear en la raiz del proyecto un archivo Dockerfile para crear la imagen:
 
# Etapa 1: Build
FROM mcr.microsoft.com/dotnet/sdk:10.0 AS build
WORKDIR /app

# Copiar csproj y restaurar dependencias
COPY *.csproj ./
RUN dotnet restore

# Copiar todo el código
COPY . ./

# Publicar la aplicación
RUN dotnet publish -c Release -o /out

# Etapa 2: Runtime
FROM mcr.microsoft.com/dotnet/aspnet:10.0
WORKDIR /app

# Copiar desde la etapa build
COPY --from=build /out .

# Exponer puerto
EXPOSE 8080

# Comando de inicio
ENTRYPOINT ["dotnet", "ProjectName.dll"]


Crear la imagen con un nombre en la raiz:
docker build -t Image-name .

Ver las imagenes:
docker images

Crear un contenedor basado en la imagen que creamos:
docker run -d -p 8084:8080 --name container-net ImageName

------------------------------------------------------------------------
dotnet tool install --global dotnet-ef

dotnet ef migration add InitialCreate

dotnet ef database update

# EF Core para MySQL
dotnet add package Pomelo.EntityFrameworkCore.MySql

# EF Core herramientas
dotnet add package Microsoft.EntityFrameworkCore.Tools
dotnet add package Microsoft.EntityFrameworkCore.Design

# MailKit para correos ← reemplaza SmtpClient
dotnet add package MailKit



1. ¿Un [A] puede tener muchos [B]?
2. ¿Un [B] puede tener muchos [A]?

Ambas sí  → Muchos a Muchos → tabla puente
Solo la 1 → Uno a Muchos   → FK en B
Ninguna   → Uno a Uno      → FK en cualquiera
